/**
 * Shared domain types for the trading dashboard.
 *
 * Add types here as later phases introduce concrete entities (trades,
 * journal entries, accounts, simulations). Keeping them centralized makes
 * the @/types alias a single import surface across the app.
 */
export {};
